<?php


function GetMemberIDFromRIN($rin){
    // global $wpdb;

    // $rin = filter_var($rin, FILTER_SANITIZE_NUMBER_INT );  // get rid of any prefix added

    // $memberResults = $wpdb->get_results($wpdb->prepare("SELECT m_email, m_rin, m_vfa, username FROM vfa_members WHERE m_rin = %d", $rin));

    // if ($wpdb->num_rows){
    //     return($memberResults[0]->username);
    // }
    // else {
    //     return NULL;
    // }


}

?>






